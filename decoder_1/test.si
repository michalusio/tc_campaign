
def get_input($scratch_space: [Int], tick: Int, $inputs: [Int]) {

    inputs[0] = tick

}

def check_output($scratch_space: [Int], tick: Int, inputs: [Int], outputs: [Int]) TestResult {
  
    switch tick
        0 {

            if outputs[0] != 1 || outputs[1] != 0 {

                return fail

            } 

        }

        1 {

            if outputs[0] != 0 || outputs[1] != 1 {

                return fail

            } 

            return win

        }

}