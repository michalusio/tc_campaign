kind = combinational
title = "Multiplexer"
size = [-30, -30, 30, 30]
unlocks_components = [com_mux]
input_labels = [["A", "B","SELECT"],["A"],["B"]]
background = 5
dialogue = [
    (alien_centered, "Even though he did not pass our tests we decided to keep the dog. Unlike most earthlings he is fluffy and can follow simple instructions. 

We might want to team you two up, since you complement each other's shortcomings well."),
    (info, "When the bit selector input is [OFF], output Byte A, otherwise output Byte B."),
]
